#include <io.h>
#include <delay.h>
#define PORTC.0 led

void main(void)
{
DDRC.0=1;
while (1)
    {
        delay_ms(200);
        led = 1;
        delay_ms(200);
        led = 0;
    }
}